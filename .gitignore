# Kiln .gitignore — Swift + Python + macOS + Xcode

# --- macOS ---
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
.fseventsd
Icon?

# --- Xcode / Swift ---
## Build artifacts
build/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xcscheme

## xcodegen output — the YAML (project.yml) is the source of truth
apps/**/*.xcodeproj/

## SwiftPM
.build/
Package.resolved
.swiftpm/
.swift-version

## Swift formatter/linter caches
.swiftformat-cache/
.swiftlint-cache/

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
.eggs/
build/
dist/
.cache/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.pyright/
*.whl

## Virtual environments
.venv/
venv/
env/
.env/

## uv
.uv-cache/

# --- Editor / IDE ---
.idea/
.vscode/
*.swp
*.swo
*~

# --- Secrets / config (NEVER commit) ---
.env
.env.*
*.env
!.env.example
*.pem
*.key
*.pat
credentials
credentials.*
.secrets/
secrets.yaml
secrets.yml
secrets.json

# --- Kiln-specific ---

## Raw Opus labels from the distillation pipeline.
## The trained artifact (manifest.json, model.*) IS committed; the raw labels are not.
distilled/*/raw_labels.jsonl
distilled/*/run_manifest.json
distilled/*/labels_cache/

## Training run outputs — large, per-user, reproducible.
runs/
checkpoints/
adapters/
fused/
*.safetensors
!distilled/*/model.safetensors
*.gguf

## Caches
.cache/
.kiln/
~/.cache/huggingface/

## Session artifacts
.claude/reviews/
.kiln-last-test-status
.kiln-demo-check.json

## Demo assets we regenerate
docs/demo/raw/
docs/demo/takes/
docs/eval/2*.md
!docs/eval/latest.md

## Managed Agent local state
managed-agents/*/state/
managed-agents/*/.cursor

## Managed Agent input JSONLs — generated deterministically; do not commit raw input
managed-agents/*/inputs/

## Managed Agent run outputs — normally gitignored by the global `runs/` rule below,
## but we opt them back in: the pilot manifest and quality-labels.jsonl are the
## judge's primary evidence for the Managed Agents prize and belong in the repo.
!managed-agents/*/runs/
!managed-agents/*/runs/**

## Logs
*.log
logs/

## OS-specific transient
*.dmg
*.pkg

# Distilled — Preference Judge

A small local model that, given a prompt and two candidate completions, returns which one better matches the user's voice. Used at DPO construction time to rank candidate rejections.

See `.claude/skills/distillation-pipeline/` for the operational recipe.

## Input / output

Input:

```json
{
  "style_signals": ["<user snippet 1>", "<user snippet 2>", "<user snippet 3>"],
  "prompt": "<single user turn>",
  "completion_a": "<candidate A>",
  "completion_b": "<candidate B>"
}
```

Output:

```json
{"winner": "A", "margin": "large", "reason": "A hedges less; matches the user's declarative cadence"}
```

- `winner ∈ {"A", "B", "tie"}`
- `margin ∈ {"large", "small"}` — used to weight DPO loss.

## Training data origin

- 5,000 triples. Half are user-vs-generic; half are adapter checkpoint-k vs checkpoint-m pairs from early dry runs.
- Labels: Opus 4.7 judgments, temperature 0.0, mirror-paired to kill ordering bias.
- Split: 80/10/10 stratified by pair source.

## Architecture

- Embedder: `sentence-transformers/all-MiniLM-L6-v2` (384-dim).
- Pair feature: `concat(emb_a, emb_b, emb_a − emb_b, emb_a * emb_b)` → 1536-dim.
- Head: scikit-learn `LogisticRegression`, predicts P(A wins).
- Shipped as scikit-learn pickle: `packages/kiln_trainer/artifacts/preference-classifier.pkl` (the SPEC §7.2 `model.mlmodel` phrasing predates M9.C; the runtime path is the pickle).

## Eval

| Metric | Bar | Current |
|---|---|---|
| Test accuracy | ≥ 0.80 | **0.9975** |
| Train accuracy | informational | 1.0 |
| Label count | informational | 2000 (1600 train / 400 test) |
| Winner distribution (A / B / tie) | balanced | 1024 / 976 / 0 |
| Ordering bias (mirrored A-rate) | ≤ 0.02 | not separately tracked — Opus rubric forces a winner so ties are zero by design |

The held-out ≥ 0.90 floor is enforced in `tests/classifiers/test_preference_trained.py`.

## Artifacts

- `packages/kiln_trainer/artifacts/preference-classifier.pkl` — shipped.
- `manifest.json` — version, git SHA, Opus teacher id, training counts, metrics, ship-bar status. Generated by `scripts/build_distilled_manifests.py`.
- `raw_labels.jsonl` — gitignored; produced by the preference-judge Managed Agent.

## Runtime use

Loaded by `kiln_trainer.classifiers.preference.score_pair_trained` when the M9.C training path needs to rank candidate completions. Never called from Swift directly.
